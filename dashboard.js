// Function to toggle the menu (slide in and slide out)
function toggleMenu() {
    const menu = document.getElementById('menu');
    menu.classList.toggle('open');
    menu.classList.toggle('closed');
}
